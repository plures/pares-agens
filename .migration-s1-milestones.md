# S1 ANALYZE — Migration Milestones (agens ↔ radix v1.49.2 → v1.55.13)

- [x] Read ADR-0022 (capability host contract)
- [x] Read ADR-0024 (canonical plugin format)
- [x] Read PLUGIN-AUTHOR-GUIDE.md
- [x] Read plugin-system.md
- [x] Explored v1.55.13 crates for new host/plugin/capability traits
- [x] Mapped old→new cli-api symbols
- [x] Mapped AuthorizationGate drift in pares-agens-core
- [x] Enumerated full blast radius (grep all agens crates for removed symbols)
- [x] Confirmed NO `[patch]` section exists in any agens Cargo.toml
- [x] Confirmed `pares-radix-migrate` has NO dep line (undeclared crate → Migrate is dead)
- [x] Confirmed tui `pares_radix_core::commands::*` survives at v1.55.13 (unaffected)
- [x] Wrote MIGRATION-PLAN.md
- [x] Inspected PR #613 (b2eb206) prior art + folded into plan (§0.5, branch strategy, §4, §7)

## Key findings (for main session)
- **PR #613 (`m6-pluresdb-px-pin-bump`, b2eb206) FOLDED IN.** It did ONLY the pluresdb-px AST bump (rev 195c67b→0ec9523) + 4 px-AST accessor deltas (runtime.rs ×2, host_runtime/mod.rs `has_scenarios` ~L541, radix_handler.rs `has_procedures` ~L2600). It did NOT bump `pares-radix-praxis` (still v1.49.2) and KEPT cli/cli-api — so the cli-api→ADR-0022 migration is UNSTARTED. Plan now targets a **FRESH branch off main that supersedes #613**, keeps `pluresdb-px 0ec9523`, re-applies #613's 4 deltas, and does the full v1.55.13 + cli-api removal on top. #613's host_runtime delta is disjoint from the cli-api edits (coexist).
- **REAL RE-ARCHITECTURE of the command-host seam, not a rename** — but small/contained.
  - `CommandProvider`/`ProviderOutcome`/`ProviderRegistry`/`CommandError`/`CommandResult` from `pares_radix_cli_api` are GONE with NO successor trait. New ADR-0022/0024 plugin model = TOML-manifest + TS/WASM VSCode-style plugin via modulus, NOT a Rust CLI-subcommand trait.
  - agens-plugin IS the host → fix = delete the cli-api indirection, call AgensProvider directly (Option B). Recommended return type: `handle -> Option<Result<(),String>>`.
- **AuthorizationGate is NOT broken by an API change.** v1.55.13 signature is byte-identical. E0599/E0053/E0308 = pin-drift type-duplication. **Fixed by unifying all radix pins to v1.55.13, ZERO code change to agens-core.**
- **Call sites to change: 4 files, all in agens-plugin** (agent_commands/mod.rs, host_runtime/mod.rs, bin/pares-agens.rs, lib.rs). 0 code changes in pares-agens-core/tui/others (pin-bump only).
- **HARD GAP:** `pares_radix_migrate::{migrate::run, openclaw::auto_detect}` (the `Migrate` subcommand) deleted, no replacement anywhere → **drop the Migrate command** (or reimplement in agens, no stubs).
- **Cargo.toml:** ~14 pin lines bump v1.49.2→v1.55.13; DROP `pares-radix-cli-api` + `pares-radix-cli`; no new radix dep to ADD.
- **Baseline unverified:** agens-plugin `use`s undeclared `pares_radix_migrate` → it may not even build at v1.49.2. S3 must `cargo check` the true baseline FIRST.
