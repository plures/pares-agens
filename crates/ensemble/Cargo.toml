[package]
name = "pares-agens-ensemble"
description = "BitNet MoE-style expert routing for Pares Agens — cerebellum gating, CPU+GPU hybrid scheduling, consensus mode, and self-tuning"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"

[dev-dependencies]
serde_json = "1"
