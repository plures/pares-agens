[package]
name = "pares-agens-service"
description = "Assistant-mode service backend for Pares Agens — systemd, launchd, and Windows Service lifecycle management"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
