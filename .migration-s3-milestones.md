# S3 IMPLEMENT — agens↔radix v1.49.2→v1.55.13 migration milestones

Branch: `m6-radix-v1.55.13-cliapi-migration` (fresh off main, supersedes #613)
Started: 2026-07-01

## Steps
- [x] 1. Cut FRESH branch off `main` — DONE. main@51a1bd3 clean, branch `m6-radix-v1.55.13-cliapi-migration` created. Broken stash left untouched.
- [x] 2. Capture TRUE baseline — DONE. `cargo check -p agens-plugin` EXIT 0; `cargo check --workspace` EXIT 0, **0 errors** at v1.49.2. Plan's "probably doesn't build" hypothesis DISPROVED (see DRIFT-1).
- [x] 3. Fold in #613 px-AST deltas — DONE. pluresdb-px 195c67b→0ec9523 in agens-plugin/core/mcp-server Cargo.toml; 4 code deltas applied VERBATIM from origin/m6-pluresdb-px-pin-bump diff (runtime.rs L403+L5532 dataflow_procedures→statements filter_map, host_runtime L544 has_scenarios, radix_handler L2603 has_procedures). Variants DataflowProcedure/Scenario/LegacyProcedure confirmed present in pluresdb-px 0ec9523 source.
- [x] 4. Pin unification — DONE. 15 radix pins bumped v1.49.2→v1.55.13 (core×9, praxis×3, rector×2, +bitnet optional core, channels rector package attr preserved). DROPPED pares-radix-cli-api + pares-radix-cli dep lines from agens-plugin. pluresdb-px stays 0ec9523. Stale comments (v1.49.2/cli/migrate refs) updated. Verified: 0 v1.49.2 pins, 0 cli/cli-api dep lines.
- [x] 5. `cargo check --workspace` post-pin — DONE. EXIT 101, exactly 5 errors, ALL in agens-plugin: 4×`pares_radix_cli_api` unresolved (mod.rs:19, host_runtime:26/30/227) + 1×`pares_radix_migrate` unresolved (host_runtime:44). **ZERO AuthorizationGate/RuleResult/E0308/E0053/E0599 errors** — plan's type-duplication root-cause CONFIRMED (pin unification fixed agens-core with zero code change). mcp-server DRIFT-2 `.statements` errors also GONE (praxis now v1.55.13). These 5 are precisely the Option-B code edits.
- [x] 6. agens-plugin code edits (Option B) — DONE. agent_commands/mod.rs (trait→inherent impl, dropped async_trait+cli_api imports, handle→Option<Result<(),String>>, panic→Err(String), NotHandled→None). host_runtime/mod.rs (deleted command_provider re-export + cli-api pub-use + `use pares_radix_migrate`, deleted Migrate variant+arm, run_with_providers now takes AgensProvider + calls augment/handle directly, PRESERVED #613 has_scenarios block in Px::Test arm). bin/pares-agens.rs (run_with_providers(AgensProvider::new()).await, dropped ProviderRegistry). lib.rs (doc reword).
- [x] 7. `cargo check -p agens-plugin` — DONE. EXIT 0 after fixing DRIFT-3 (runtime.rs CompositeActionHandler::new signature drift). See DRIFT-3.
- [x] 8. `cargo check --workspace` green + clippy — DONE. **`cargo check --workspace` EXIT 0, 0 errors.** clippy ENV-BLOCKED locally (`clippy-driver.exe ... os error 2 The system cannot find the file specified` — the known local toolchain gap; CI is lint authority per task). No unused-import warnings from my edits; remaining warnings are all pre-existing dead-code in cli/main.rs + bitnet_classifier.rs (present at v1.49.2 baseline, out of scope). Verified ZERO live cli_api/migrate/Provider*/CommandError residue (only benign doc-comments) and ZERO stub markers.
- [x] 9. BUILD + RUN binary — DONE. `cargo build -p agens-plugin --bin pares-agens` EXIT 0 (target\debug\pares-agens.exe). `--help` surface = cluster/mcp-serve/config/px/serve-spine/serve/tui/ask (+help); **`migrate` GONE** ✓ (classify is bitnet-native cfg-gated, not in this build — honest absence). `px check autonomous-dispatch.px` → `✓ 1 file(s) checked, 0 error(s)` EXIT 0. `px check .migration-smoke.px` → `✓` EXIT 0. `px test .migration-smoke.px` → `✓ smoke_scenario_present / ✓ 1 scenario(s) passed` EXIT 0 — directly exercises #613 has_scenarios delta on v1.55.13 praxis + pluresdb-px 0ec9523.
- [x] 10. Regenerate Cargo.lock — DONE (relocked automatically by the check/build for v1.55.13 tags + pluresdb-px 0ec9523; not hand-merged).
- [x] 11. Commit on fresh branch — DONE. Commit `e936642` on `m6-radix-v1.55.13-cliapi-migration`, 17 files (+189/-180). Conventional message notes it SUPERSEDES #613. NOT pushed, NO PR (gated to parent). Working tree clean of tracked changes (only working-artifact MDs untracked).

## FINAL STATUS: COMPLETE ✅
- Branch: `m6-radix-v1.55.13-cliapi-migration`  |  Commit: `e936642`
- `cargo check --workspace`: **EXIT 0, 0 errors**
- `cargo build -p agens-plugin --bin pares-agens`: **EXIT 0**
- `pares-agens --help`: migrate GONE (surface: cluster/mcp-serve/config/px/serve-spine/serve/tui/ask)
- `px check` + `px test` (scenario): **both pass** (folded #613 path verified on v1.55.13)
- clippy: env-blocked locally (clippy-driver os error 2); CI is authority
- Residue: ZERO live cli_api/migrate/Provider*/CommandError; ZERO stubs. Migrate cleanly removed (C-NOSTUB-001 ✓)
- 3 drift items recorded (DRIFT-1 baseline correction, DRIFT-2 #613-coupling, DRIFT-3 CompositeActionHandler::new sig)

## Drift fixes beyond plan

### DRIFT-1 (BASELINE CORRECTION, not a code fix): plan §4‡ rationale was WRONG
- Plan claimed `pares_radix_migrate` is an UNDECLARED crate and the tree "probably doesn't build clean at v1.49.2".
- **REALITY (empirically verified):** `cargo check -p agens-plugin` and the full workspace BOTH build CLEAN at v1.49.2 (exit 0, forced recompile confirmed).
- **Root cause of the confusion:** at v1.49.2 the package `pares-radix-cli` declares `[lib] name = "pares_radix_migrate"` (checkout bf765f7 crates/cli/Cargo.toml). So `use pares_radix_migrate::{migrate, openclaw};` resolves to the `pares-radix-cli` package's lib (submodules crates/cli/src/{migrate.rs,openclaw.rs}). It is DECLARED (via the `pares-radix-cli` dep line 32), just under a surprising lib name.

### DRIFT-2 (plan step-3 "verify #613 green on v1.49.2" is IMPOSSIBLE — deltas are coupled to the praxis bump)
- Plan §7 step 3 says: after folding #613 (pluresdb-px→0ec9523 only, radix still v1.49.2), `cargo check --workspace` should "confirm #613 reproduces green."
- **REALITY:** it does NOT and CANNOT. Result was EXIT 101, 3 errors, ALL in mcp-server/radix_handler.rs:
  - `error[E0609]: no field 'statements' on type pares_radix_praxis::px::PxDocument`
  - `error[E0433]: cannot find 'Statement' in 'px'` (×2)
- **Root cause:** with praxis pinned at v1.49.2, `pares_radix_praxis::px` glob-re-exports `pluresdb_px 195c67b`'s px (the OLD flattened-vector `PxDocument` with `.procedures`/`.scenarios`/`.dataflow_procedures`, NO `.statements`, NO `Statement` enum). The graph even carries TWO pluresdb-px versions at once (praxis→195c67b, agens→0ec9523). The `.statements`/`Statement` shape only exists at v1.55.13 praxis (which re-exports pluresdb_px 0ec9523).
- **Conclusion:** #613's px-AST deltas are INSEPARABLE from the praxis version bump. #613 as a standalone branch (radix pins still v1.49.2) was itself non-compiling for its own radix_handler delta — it was an incomplete WIP that only becomes valid once praxis moves to v1.55.13. This is exactly why the plan folds #613 INTO the v1.55.13 unification rather than shipping it alone. No code change needed — proceeding straight to Step 4 (pin unification) resolves these 3 errors.

### DRIFT-3 (REAL forced code fix): runtime.rs `CompositeActionHandler::new` signature drift v1.49.2→v1.55.13
- Plan §6.3 predicted "a handful of signature touch-ups in runtime.rs." This is the one.
- **Error:** `runtime.rs:3738 E0061` (fn takes 3 args, 2 supplied) + `:3740 E0308` (expected `&Arc<dyn StateStore>`, found `&Arc<ToolDispatchActionHandler>`).
- **Ground truth (read from checkout, NOT guessed):** v1.55.13 `pares_radix_core::spine::actions::CompositeActionHandler::new` (actions.rs:198) signature is now:
  `new(conversation_store: Arc<dyn ConversationStore>, state_store: Arc<dyn StateStore>, tool_handler: Arc<ToolDispatchActionHandler>)` — a NEW `state_store` param inserted at position 2. (v1.49.2 was 2-arg: conversation_store + tool_handler.)
- **Canonical wiring pattern (also read from checkout):** radix-core `spine/runtime.rs:158` builds it as `new(Arc::clone(&conversation_store), Arc::clone(&state_store), tool_handler)`; and `spine/runtime.rs:202-206` shows the intended store sharing: `let pdb = PluresDbStateStore::open(dir); PluresConversationStore::new(pdb.crdt_store())` — ONE CrdtStore shared between the durable state store and the conversation store.
- **Fix applied (public API only, behavior-preserving, co-location invariant kept):** restructured runtime.rs §3.5 store-open (was `SledStorage::open` → raw `CrdtStore`) to build `PluresDbStateStore::open(&pluresdb_dir)` (fallback `::in_memory()`), extract `shared_store = pdb.crdt_store()` (inherent method — must be called on the concrete `PluresDbStateStore` BEFORE erasing to `Arc<dyn StateStore>`, since `crdt_store()` is NOT on the `StateStore` trait), then pass `Arc::clone(&state_store)` as arg 2 to `CompositeActionHandler::new`. conversation_store still derives from the same `shared_store`, so state co-locates exactly as before. NO new/invented API; NO stub.
- **Net runtime.rs change:** the §3.5 store-open block + the one `CompositeActionHandler::new` call site. `SledStorage`/`StorageEngine` imports in that block replaced by `PluresDbStateStore`/`StateStore` (SledStorage is now opened internally by `PluresDbStateStore::open`).


## Notes
(running log)
